/**
 * Bootstrap Theme Blocks
 * JavaScript para todos los bloques de Bootstrap
 * 
 * @package Bootstrap_Theme
 */

(function() {
    'use strict';
    // Intentionally left as lightweight fallback bootstrap.
    // Block registrations are handled in each block's dedicated editor.js file.

})();